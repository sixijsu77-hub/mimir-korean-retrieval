# MIMIR dependencies, installed in stages rather than all at once.
# Python 3.10.12. Versions are pinned so the numbers in docs/ reproduce exactly.

# --- Stage 1: dataset access and measurement --------------------------------
# `datasets` is the documented way to load the MTEB-ko sets; `huggingface_hub`
# is used directly to pin file revisions and read repo sizes without downloading.
datasets==5.0.1
huggingface-hub==1.27.0
pyarrow==25.0.1
numpy==2.2.6

# --- Stage 2: BM25 retrieval ------------------------------------------------
# bm25s is the same engine MTEB's published baseline uses, so a mismatch against
# published numbers points at this harness rather than at the BM25 scoring.
# PyStemmer supplies the English stemmer MTEB applies to Korean text.
bm25s==0.3.10
PyStemmer==3.1.0

# --- Stage 3: Korean morphological analysis (exp04) -------------------------
# Kiwi is the analyser behind the `morph` and `morph_content` tokenizers. Not
# needed for any character- or word-level result.
kiwipiepy==0.23.2
kiwipiepy_model==0.23.0

# --- Stage 4: dense retrieval (week 2) --------------------------------------
# Not needed to reproduce any BM25 result in docs/results-week1.md — skip these
# lines if you only want the week-1 numbers. The PyPI Linux wheel bundles its own
# CUDA runtime, so no CUDA toolkit install is required.
torch==2.13.0
sentence-transformers==5.7.0
transformers==5.15.0
